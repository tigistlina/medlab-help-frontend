import React from "react";
import PropTypes from "prop-types";

const Panel = (props) => {
    return (
        <ul>
            <li>{props.name}</li>
        </ul>
       
    )
}

export default Panel;